Changelog for TekTones:

TekTones v3 is a fully featured subtractive synthesizer and sequencer workstation that runs entirely in the browser. 
Unlike many web synths that rely on wrapper libraries (like Tone.js), TekTones interacts directly with the Web Audio API via a custom useAudioEngine hook, 
offering low-latency performance and granular control over the audio graph.
